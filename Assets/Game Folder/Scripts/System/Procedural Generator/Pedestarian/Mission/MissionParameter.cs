using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "Mission Parameter", menuName = "Procedural generator / Mission Parameter", order = 1)]
public class MissionParameter : BaseGeneratorParam
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
